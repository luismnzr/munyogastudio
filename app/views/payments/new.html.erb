<div class="containerPrices">
    <h2>Planes y precios </h2>
    <br>
    <p class="button-orange-width">No necesitas hacer reservación previa para nuestras clases. Solo llega con tiempo, elige tu espacio y disfruta de tu práctica. Recuerda que tu plan tiene una vigencia de 30 días a partir de la fecha de pago.</p>
    <br>
    <%= image_tag("lotus.svg") %>
    <div class="container containerPrices-grid">
        <div class="containerPrices-item">
            <h2>1 Clase</h2>
            <br>
            <p>$280</p>
            <br>
            <% if user_signed_in? %>
                <%= button_to 'Comprar Clase', payments_create_path(plan_id: Rails.configuration.stripe[:prices][:single]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
            <% else %>
                <%= link_to 'Inicia sesión para comprar', new_user_registration_path, class: 'button-orange' %>
            <% end %>
        </div>
        <div class="containerPrices-item">
            <h2>4 Clases</h2>
            <br>
            <p>$1,100</p>
            <br>
            <% if user_signed_in? %>
                <%= button_to 'Comprar Clases', payments_create_path(plan_id: Rails.configuration.stripe[:prices][:double]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
            <% else %>
                <%= link_to 'Inicia sesión para comprar', new_user_registration_path, class: 'button-orange' %>
            <% end %>
        </div>
        <div class="containerPrices-item">
            <h2>8 Clases</h2>
            <br>
            <p>$1,500</p>
            <br>
            <% if user_signed_in? %>
                <%= button_to 'Comprar Clases', payments_create_path(plan_id: Rails.configuration.stripe[:prices][:mid]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
                <br>
                <%= button_to 'Pago Recurrente', payments_recurring_path(plan_id: Rails.configuration.stripe[:recurring_prices][:mid]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
            <% else %>
                <%= link_to 'Inicia sesión para comprar', new_user_registration_path, class: 'button-orange' %>
            <% end %>
        </div>
        <div class="containerPrices-item">
            <h2>12 Clases</h2>
            <br>
            <p>$1,950</p>
            <br>
            <% if user_signed_in? %>
                <%= button_to 'Comprar Clases', payments_create_path(plan_id: Rails.configuration.stripe[:prices][:full]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
                <br>
                <%= button_to 'Pago Recurrente', payments_recurring_path(plan_id: Rails.configuration.stripe[:recurring_prices][:full]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
            <% else %>
                <%= link_to 'Inicia sesión para comprar', new_user_registration_path, class: 'button-orange' %>
            <% end %>
        </div>
        <div class="containerPrices-item">
            <h2>Ilimitado</h2>
            <br>
            <p>$2,300</p>
            <br>
            <% if user_signed_in? %>
                <%= button_to 'Comprar Clases', payments_create_path(plan_id: Rails.configuration.stripe[:prices][:limitless]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
                <br>
                <%= button_to 'Pago Recurrente', payments_recurring_path(plan_id: Rails.configuration.stripe[:recurring_prices][:limitless]), method: :post, remote: true, data: { turbo: false }, class: 'button-orange' %>
            <% else %>
                <%= link_to 'Inicia sesión para comprar', new_user_registration_path, class: 'button-orange' %>
            <% end %>
        </div>

    </div>
</div>